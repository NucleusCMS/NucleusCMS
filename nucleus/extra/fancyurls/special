<?php
global $special;

include('./fancyurls.config.php');
include('./config.php');

if ( !requestVar('special') )
{
	$data = preg_split("#/#", serverVar('PATH_INFO'));
	if ( count($data) > 1 )
	{
		$special = $data[1];
	}
}

selector();
